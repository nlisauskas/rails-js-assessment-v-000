<h1>Welcome to <%= @course.name %>.</h1>

<%= render 'info' %>

<%= link_to "Take a look at all of the rounds that have been played at #{@course.name}.", course_rounds_path(@course) %><br></br>

<%= link_to "Take a look at all of the golfers that have played at #{@course.name}.", course_users_path(@course) %>
<br></br>
<%= link_to "Played here? Add a round.", new_round_path %>

<br></br><a href="#" class="nextCourse" data-id="<%= @course.id %>"><strong>Next Course</strong></a>
<!-- Need to render next course (add next button) using jQuery and Active Model Serialization JSON Backend.
Fetch and render next course via JQuery / AJAX. -->

<script type="text/javascript" charset="utf-8">
$(function () {
  $(".nextCourse").on("click", function() {
    $.get("/courses/" + $(".nextCourse").attr("data-id") + ".json", function(data) {
      for (var i = 0; i < data.length; i++) {
        //Figure out how to get this code right to flip to next course
        //rounds += `<div><strong>Round Number: ${i + 1}</strong></div><br>`
        //rounds += `<div>Course Name: ${data[i]["course"]["name"]}</div><br>`
        //rounds += `<div>Score: ${data[i]["score"]}</div><br>`
        //rounds += `<div>Putts: ${data[i]["putts"]}</div><br>`
      }
    });
    event.preventDefault();
  });
});
</script>
